import 'package:flutter/material.dart';

class AppColors {
  // =======================
  // Grayscale
  // =======================

  static const white   = Color(0xFFFFFFFF);
  static const bg      = Color(0xFFF9F9F9);

  static const gray00 = Color(0x00000000); //투명
  static const gray01  = Color(0xFFF5F5F5); //gray50
  static const gray02  = Color(0xFFE0E0E0); //gray100
  static const gray03  = Color(0xFFBDBDBD); //gray300
  static const gray04  = Color(0xFF9E9E9E); //gray500
  static const gray05  = Color(0xFF616161); //gray700

  static const black   = Color(0xFF424242); //gray900

  // =======================
  // Font Colors
  // =======================
  static const f01     = Color(0xFFFFFFFF);
  static const f02     = Color(0xFF999999);
  static const f03     = Color(0xFF767676);
  static const f04     = Color(0xFF505050);
  static const f05     = Color(0xFF111111);
  static const fMain   = Color(0xFFF9CA3C);

  // =======================
  // Main Brand Color
  // =======================
  static const main    = Color(0xFFFFC400);

  // =======================
  // System Colors
  // =======================
  static const red     = Color(0xFFFF4343);
  static const blue    = Color(0xFF007AFF);
}